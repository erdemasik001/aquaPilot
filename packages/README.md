# packages/

Shared libraries and non-app packages (npm workspaces). Each package lives in its own directory
with a `package.json`.

Planned:

- `contracts/` — Solidity contracts (`CampaignEscrow`, `PayoutVault`) + Hardhat
- `shared/` — shared TypeScript types and utilities

Added incrementally via `feat/*` pull requests.
